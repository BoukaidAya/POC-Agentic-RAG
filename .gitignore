# Regenerable : uv run python embed_chunks.py (~45 min sur CPU).
# 117 Mo pour le corpus complet -- au-dessus de la limite de 100 Mo de GitHub.
extracted/embeddings/

# Logs de debug locaux, pas du contenu du projet
*.log

__pycache__/
*.pyc

# Fichiers temporaires d'editeurs (draw.io : .$*.dtmp / .bkp ; Word : ~$*)
.$*
*.dtmp
*.bkp
~$*
*.tmp

# Environnement virtuel local
.venv/

# Cles/secrets -- .env.example (le modele, sans vraie cle) reste versionne
.env
